# backend

1) Use npm install 
and then
2) Use npm start to run the project

Project will run at http:localhost:4000/

To test the first task: http://localhost:4000/api/checkPrime/2

To test the 2nd task: http://localhost:4000/api/checkPrime
It is done using POST method
So it can not be shown in the URL
